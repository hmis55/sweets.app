<%= form_with model: [post, post_comment], class: "mt-3", local: false do |f| %>
  <%= f.text_area :comment, rows: '5', class: "form-control", placeholder: "コメントをここに記入してください" %>
  <%= f.submit "送信する", class: "btn btn-primary mt-2" %>
<% end %>